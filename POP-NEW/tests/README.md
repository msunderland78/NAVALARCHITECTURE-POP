# POP Tests

This directory contains fixtures and expected outputs recovered from legacy POP artifacts.

`fixtures` contains normalized inputs for the new calculation model.

`golden` contains expected outputs used to verify the new implementation.
